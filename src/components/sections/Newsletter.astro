---

---

<section
  class="py-24 bg-[var(--color-surface)] border-t border-[var(--color-border)]"
>
  <div class="max-w-xl mx-auto px-6 text-center">
    <div
      class="i-ph-envelope-open-duotone text-5xl text-[var(--color-secondary)] mx-auto mb-6"
    >
    </div>
    <h2 class="text-3xl font-serif font-bold mb-4 text-[var(--color-primary)]">
      La Carta Semanal
    </h2>
    <p class="mb-8 text-[var(--color-text)] opacity-70 leading-relaxed">
      Reflexiones curadas sobre fe, cultura y la vida intelectual. Sin spam,
      solo sustancia.
    </p>
    <form class="flex flex-col sm:flex-row gap-3">
      <input
        type="email"
        placeholder="correo@ejemplo.com"
        class="flex-grow px-4 py-3 bg-[var(--color-bg)] border border-[var(--color-border)] rounded-sm text-[var(--color-primary)] focus:outline-none focus:border-[var(--color-secondary)] transition-colors"
      />
      <button type="submit" class="btn">Suscribirse</button>
    </form>
  </div>
</section>
